# Utilise par `vite build` (mode "production" par defaut), prioritaire sur .env.
# .env reste sur localhost:8081/api pour le dev local (npm run dev) ; ce fichier
# ne s'applique qu'aux builds de production (image Docker deployee).
VITE_API_URL=https://api.myedugame.me/api
